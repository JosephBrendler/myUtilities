echo 
echo "NEXT STEPS"
echo "Note: this file is designed to be sourced in a joetoo environment"
echo "Re-read at any time with e.g. source /etc/crossbuild-tools/README_next_steps_after_env"
echo
    message "${BYon}Environment creation is complete, but there is more you can do...${Boff}"
    message ""
    message "${LBon}You are basically running natively in a minimal gentoo system. You may want to try these${Boff}"
    message "  ${LBon}steps before you exit the chroot and stockpile the image again${Boff} -"
    message "  + ${LBon}<TBD>${Boff}"
    echo
    message "${BWon}To do: some of the above may be automated by additional tools...${Boff}"
echo 
echo "NEXT STEPS"
echo "Note: this file is designed to be sourced in a joetoo environment"
echo "Re-read at any time with e.g. source /etc/crossbuild-tools/README_next_steps_after_update"
echo
