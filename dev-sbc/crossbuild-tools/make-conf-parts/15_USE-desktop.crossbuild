# joetoo standard global USE flags (desktop)(crossbuild) (20250716)
# use this (line below) for chroot and live system
#USE=" \
# use this (line below) for crossbuild, replace with above (uncommented) if chroot
USE=" -pam -acl \
  ${ARCH} apache2 caps cgi crypt cxx dbus dvd evdev gd gif git gnutls gpm \
  introspection libnotify logrotate mp3 mpeg mtp mysql odbc ogg pam \
  pdf perl pic pie png policykit ssl threads tiff truetype udev unicode \
  upnp urandom usb verify-sig zlib X gtk gtk2 kde plasma qt6 \
  -bindist -debug -networkmanager -samba -systemd -test -webengine \
"
