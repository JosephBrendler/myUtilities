for lv in $(lvs -o lv_name $root_vg --noheadings | grep -v 'root\|swap\|extra')
do
  [ ! -d ${mountpoint%/}/${lv} ] && echo "mkdir ${mountpoint%/}/${lv}..." && mkdir ${mountpoint%/}/${lv}
  mount /dev/mapper/${root_vg}-${lv} ${mountpoint%/}/${lv}
done
