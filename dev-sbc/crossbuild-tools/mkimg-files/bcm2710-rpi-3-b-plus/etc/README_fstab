after formatting and mounting partitions of an external media (e.g. SD card)  --

use blkid to get UUID or PARTUUID data for the new device (not the image file) and populate fstab

also add an entry for the swap device at /var/cache/swap/swap1
