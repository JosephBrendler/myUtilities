#!/bin/bash
# source collect-system-files - specific usage-module which should be built
# in the same format. This file must contain legitimate bash assignments and
# commands it will be sourced by the using script via the sourcing of
# script_header_joetoo_extended usage() function
# Note: options i, n, s, r, v, q are already used by the default cmdline processor
echo
message "${BYon}Copy/create/set configuration (/etc/${PN}/${PN}.conf)${Boff}"
message "${BYon}using ${BGon}eselect$ ${PN} ${BWon}list|set n${Boff}"
