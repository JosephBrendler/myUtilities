# ddns_updater - provides client and dns server scripts to support ddns for ipv6
#   in a dual-stack environment including both direct dnsmasq/radvd clients and openvpn clients
#
# basic plan - (LEGACY through ver 0.0.10) packaged with USE = exactly one of (client, dns)
# and if client, then USE = exactly one of (vpn, direct, daemon)
# - for dns: install dns directory
# - for client: if direct, install client directory
#               if vpn, install only 99_ula_nei_fix
# (above accomplised about 30 March 2026)
#
# new plan - (ver 0.0.10 - ) develop openrc service init script (ddns),
#     ddns-daemon, and ddns-update scripts and migrate to using them
#     (above accomplished about 15 Arpil 2026)
# 
# new plan - ( 0.1.0 - 0.1.10 ) dual-stack environment including both direct dnsmasq/radvd clients and openvpn clients
# - as of version 0.1.0 20260424, drop vpn, direct, daemon use flags
# - for dns: install dns directory iaw README_dns
# - for client: install new ddns service, ddns-daemon, and ddns-update iaw README_client
# - also then move elrond's server-side script from /etc/ddns-update to /usr/sbin
# - as of ver 0.1.10 20260430, complete with migration of all legacy ipv4 and ipv6
#   client support for dhsmasq/SLAAC clients and successfully upgraded
#   elrond (router)'s openVPN_dns_updater.sh to validate connectivity
#   to hosts found in openvpn-status.log ROUTES, using "timeout 0.3 nc -zv $host 22"
#   before adding them
#
# new plan  ( 20260508 - tbd )
# - start splitting ipv4 and ipv6 in every module
#   tbd: need to decide if client side use of ovpn up hooks is feasible, what to do about down
#        or whether to just migrate openVPN_dns_updater.sh --> emaint_ddns to handle
#        all ovpn clients from the server side AND 
#        do periodic check/flush for the dnsmasq/SLAAC side
# - rename dns USE flag to server - ver 0.2.0
# - rename package to net-misc/ddns - ver 0.2.6
# - implement openvpn_4 and openvpn_6 (client side ddns updates from vpn) - ver 0.2.8
# - implement server-side vpn-clent disconnect hook - ver 0.3.0
# - implement server-side ddns maintainer - TBD
#
# also to continue retiring legacy scripts from older versions
#  (to-do: legacy id_ddns_update ssh key)
#
