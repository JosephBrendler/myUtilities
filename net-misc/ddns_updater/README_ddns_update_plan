# ddns_updater - provides client and dns server scripts to support ddns for ipv6
#   in a dual-stack environment including both direct dnsmasq/radvd clients and openvpn clients
#
# basic plan - package with USE = exactly one of (client, dns)
# and if client, then USE = exactly one of (vpn, direct)
# - for dns: install dns directory
# - for client: if direct, install client directory
#               if vpn, install only 99_ula_nei_fix
#
# after implementation, remove pieces from joetoolkit
#
# above accomplised about 30 March 2026
#
# new plan - develop rc service init script (ddns-svc) , ddns-daemon, and ddns-update scripts
#   and migrate to using them when stable, then move elrond's server-side script from /etc/ddns-update to /usr/sbin
