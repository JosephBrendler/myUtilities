# dhcpcd.conf - install to /etc/ (this is dns/router version of the .conf)

# openvpn_dns_updater.sh - install to /usr/sbin/
# 20_openVPN_clients - gets built in /etc/hosts.d/ by openvpn_dns_updater.sh

# 30_ddns_clients - gets build in /etc/hosts.d/ by update-client-host.sh

# 99-ddns-update - install to /etc/sudoers.d/ (allows remote connect to run specified commands)

# update-client-host.sh - install to /etc/ddns_update/ (this is the script run by sudo when the ssh
#   user connects and sends "${COMMAND_ARGS} (="${ACTION_IPV6_ADDRESS} ${FQDN} ${INTERFACE} $(timestamp)")

# note: router must have /home/joe/.ssh/id_ddns_update.pub (public key)
# note: client connects to submit update with /home/joe/.ssh/id_ddns_update (private key_

# note that /etc/ddns_update/update-client-host.sh is run by the remotely connected updater
#   and it uses LOCK_DIR="/run/lock/ddns_updater"
#   and keeps debug log in /tmp/dhcpcd.ddns-update.debug.log
#   script collects the arg sent by ssh args and writes to temp file and then sorts output
#   and prints fixed width cols to hosts file

# *** To Do ***
# (1) migrate clients from dhcpcd.ddns_update.sh to ddns_watcher service/daemon/updater
# (2) migrate dns server script from /etc/ddns_update/update-client-host.sh to 
#     /usr/sbin/ with /home/${user}/authorized_keys pointing to the new executable pathname
