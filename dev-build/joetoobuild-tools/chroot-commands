# note: it is essential to clear the environment when chrooting,
# else the chroot inherits livecd's and fails to set it up properly,
# so that (for example) /usr/sbin will be in the PATH and
# /root/.bashrc can run properly
env -i chroot /mnt/gentoo /bin/bash
source /etc/profile
export PS1="(chroot) $PS1"


# consider adopting a more explicit chroot command:
#env -i HOME=/root TERM="$TERM" PATH=/usr/bin:/bin:/usr/sbin:/sbin chroot /mnt/gentoo /bin/bash -l
# consider updating an EARLY line in .bashrc to set PATH rather than putting it at the end of bashrc_joe_brendler...

