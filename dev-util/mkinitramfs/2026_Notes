# add a redirection at the very beginning of the init script to broadcast to both the serial and HDMI consoles: 
# Redirect all output to both HDMI and Serial during initramfs
exec > /dev/tty1 2>&1
# Alternatively, to send to both if your environment supports it:
# exec > /dev/console 2>&1
