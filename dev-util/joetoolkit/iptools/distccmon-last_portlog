last_portlog=$(ls -Art /var/log/portage/*.log | tail -n1);
 echo;
 echo $last_portlog;
 echo $(repeat '-' ${#last_portlog});
tail -f $last_portlog | grep --line-buffered -E 'completed ok' | \
 awk '{ n=split($3, path, "/"); print "[" strftime("%H:%M:%S") "]", path[n], $4, $5; fflush()}
