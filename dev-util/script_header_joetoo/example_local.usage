#!/bin/bash
# source user-script specific usage-module which should be built in the same format
# this file must contain legitimate bash assignments and commands
# it will be sourced by the script_header_joetoo_extended usage() function
# Note: options i, n, s, r, v, q are already used by the default cmdline processor
message "  -e | --example ..........: example local option"
message "  -d | --defaults .........: example local option"
message "  -o | --output ...........: example output operand "
echo
message "${BMon}Note:${Boff}"
message "${BYon}(1) -o cannot be combined with other single-letter options${Boff}"
message " (example -ivo <operand> will fail b/d of ${BRon}-ivo${Boff}"
message "${BWon}the word following -o|--output will be treated as its operand${Boff}"
message "${BRon} minimal usage: ${BGon} ${PN} ${BMon}-o ${LBon}<OPERAND>${Boff}"
